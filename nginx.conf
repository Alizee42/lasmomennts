server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Toutes les routes Angular → index.html (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache statique
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
